package  {	import flash.display.MovieClip;	import flash.events.Event;		public class competidores extends MovieClip {		public var xInicial:Number = 50;		public var yInicial:Number = 50;		public var velocidade:Number = 2;		public var distX		public var distY		public var angulo_rad				public function competidores() {			this.x = xInicial;			this.y = yInicial;						// MOVE			addEventListener(Event.ENTER_FRAME, move_interno);		}				private function move_interno(evt:Event):void {							//trace("X: " + this.x);			//trace("Y: " + this.y);			/*			if(this.x < 360 && this.y <= 50){				this.x += this.velocidade;			} else if(this.x >= 360 && this.y < 360){				this.y += this.velocidade;			} else if(this.x >= 50 && this.y <= 360){				this.x -= this.velocidade;			}  else if(this.x <= 50 && this.y <= 360){				this.y -= this.velocidade;			} 			*/			distX = MovieClip(parent).trackPoint1.x - this.x			distY = MovieClip(parent).trackPoint1.y - this.y			angulo_rad = Math.atan2(distY,distX)			var xVel=Math.cos(angulo_rad)*velocidade;			var yVel=Math.sin(angulo_rad)*velocidade;			this.y +=yVel;			this.x +=xVel;		}	}	}